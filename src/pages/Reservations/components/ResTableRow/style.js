import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  pending: {
    color: '#f9a825',
  },
  approved: {
    color: '#2e7d32',
  },
  rejected: {
    color: '#b71c1c',
  },
});

export default useStyles;
